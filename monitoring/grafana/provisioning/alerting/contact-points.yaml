apiVersion: 1

contactPoints:
  - orgId: 1
    name: quickticket-alerts
    receivers:
      - uid: quickticket-webhook
        type: webhook
        settings:
          url: http://host.docker.internal:9099/
          httpMethod: POST
